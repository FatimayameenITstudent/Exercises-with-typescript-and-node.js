"use strict";
let names = ['Yahya', 'usman', 'wali', 'Sana', 'yameen'];
const message = "Welcome to typescript class: ";
for (let i = 0; i < names.length; i++) {
    console.log(message + names[i]);
}
